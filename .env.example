TMDB_API_KEY=your_key_here

# 运行端口 (默认 3000)
PORT=3000
# (可选) 大陆用户 TMDB 反代地址
# 如果您的服务器部署在大陆或主要面向大陆用户，请配置此项
# 部署说明见 cloudflare-tmdb-proxy.js
TMDB_PROXY_URL=

# Cache Type: 'json' (default), 'sqlite', 'memory', or 'none'
CACHE_TYPE=json

# (可选) 访问密码 - 设置后需要输入密码才能使用
# 密码使用 SHA256 加密后存储在客户端 localStorage
ACCESS_PASSWORD=

# (可选) 远程配置文件地址
# 如果设置了此项，将从远程URL加载 db.json 配置
# 留空则使用本地 db.json 文件
REMOTE_DB_URL=
